---
paths:
  - ".git/**"
  - ".github/**"
description: Git操作・ブランチ戦略・コミット規約（コミット・PR作成時にロード）
---

# Gitワークフロー

## ブランチ戦略
- ベースブランチ: main
- Devブランチ: 複数機能をまとめる作業用ブランチ。命名規則・PR先はプロジェクトごとにCLAUDE.mdまたはCLAUDE.local.mdで定義する
- 機能ブランチ: feature/[Spec ID]（例: feature/REQ-AUTH-001）

## コミットメッセージ規約

| 段階 | プレフィックス | 例 |
|------|-------------|-----|
| 設計書作成 | `docs:` | `docs: REQ-XXX-NNN 要件定義書を作成` |
| 実装 | `feat:` | `feat: REQ-XXX-NNN CRUD APIの実装` |
| 実装自己レビュー | `refactor:` | `refactor: REQ-XXX-NNN コード品質改善` |
| レビュー対応 | `fix:` | `fix: REQ-XXX-NNN レビュー指摘対応` |
| UT（ユニットテスト） | `test(ut):` | `test(ut): REQ-XXX-NNN Usecaseのユニットテスト追加` |
| ST（システムテスト/E2E） | `test(st):` | `test(st): REQ-XXX-NNN E2Eテスト追加` |

形式: `[プレフィックス]: [Spec ID] [変更概要]`

## PR本文の生成環境記載

Claude Code で PR を作成するときは、PR 本文に「生成環境」ブロック（モデル・エフォート・Claude Code バージョン）を必ず含める。どのモデル・エフォートで生成された変更かを後から追跡できるようにするため。

### 手順

1. `.claude/hooks/pr-env-metadata.sh` を実行する
2. その出力（`claude-env` マーカー行から表まで）を、PR 本文のテンプレート該当箇所と丸ごと置き換える
3. `gh pr create --body-file` を使う場合も、本文ファイルに同じブロックを含める

テンプレート: `.github/pull_request_template.md`

### ルール

- 値は必ずスクリプトの出力を使う。スクリプトはセッションのトランスクリプトから実測値を読むため、記憶や推測で書き換えない
- スクリプトが「不明」を返した項目は「不明」のまま残す
- 人間が手動で作成する PR ではこのセクションを削除してよい

## ブランチ状態の定義

機能別ブランチ対応表で使用する状態ラベル。

| 状態 | 意味 |
|------|------|
| `設計中` | 設計書を作成中 |
| `実装中` | コード実装中 |
| `自己レビュー中` | 実装完了後の自己レビュー・リファクタリング中 |
| `レビュー待ち` | レビュアーにレビューを依頼中 |
| `レビュー対応中` | レビュー指摘の修正対応中 |
| `UT中` | ユニットテスト作成・実行中 |
| `ST中` | システムテスト（E2E・手動テスト）作成・実行中 |
| `PR済み` | DevブランチまたはmainへのPR作成済み |
| `マージ済み` | Devブランチまたはmainにマージ完了 |
| `完了` | 全作業完了（ブランチ削除済み） |
